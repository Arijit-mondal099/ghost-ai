# Progress Tracker

Update this file whenever the current phase, active feature, or implementation state changes.

## Current Phase

- Foundation done. Editor chrome built. Authentication wired. Project CRUD surface (dialogs + sidebar actions) built. Prisma data models, client singleton, and first migration applied. Next: wire `hooks/use-project-dialogs.ts` to the new Prisma client (replace `mockProjects` with `prisma.project.findMany`, derive `ownerId` from `auth()`, swap the three `submit*` bodies for Prisma mutations) and add the `app/api/projects` route handlers the hook will call. The real-time canvas surface (Liveblocks + React Flow) is a later spec.

## Current Goal

- Swap the project list + dialogs from mock data to the Prisma store (Postgres + Clerk-derived `ownerId`): server actions or API route handlers for create / rename / delete, hook consumers stay untouched, build + lint + typecheck pass.

## Completed

- **Spec 01 — Design system & shadcn/ui foundation** (commit pending)
  - `shadcn@4.19.1` and `lucide-react@1.38.0` installed as devDependencies.
  - `shadcn init` (radix-nova preset, Tailwind v4, neutral base, CSS variables) — wrote `components.json`, `lib/utils.ts` (`cn()`), updated `app/globals.css`, and pre-installed `button.tsx`.
  - `app/globals.css` rewritten with the project's dark-theme token system: shadcn-compatible aliases (`--background`, `--foreground`, `--primary`, etc.) mapped to the project's hex values from `ui-context.md`, plus the project-specific tokens (`--bg-base`, `--bg-surface`, `--text-primary`, `--accent-primary`, `--accent-ai`, etc.) exposed as Tailwind utilities via `@theme inline` (`bg-base`, `text-copy-primary`, `text-brand`, `bg-accent-dim`, etc.).
  - `app/layout.tsx` updated to add the `dark` class to `<html>` so shadcn's `dark:` selectors apply.
  - Seven shadcn primitives installed in `components/ui/`: `button`, `card`, `dialog`, `input`, `tabs`, `textarea`, `scroll-area`. All generated by the CLI and unmodified per the protected-component rule.
  - `.oxfmtrc.json` `ignorePatterns` extended with `.oxlintrc.json`, `CLAUDE.md`, and `commitlint.config.js` to work around pre-existing CRLF line endings in those files (separate from design-system work; see Open Questions).
  - Smoke-tested: `next build` compiles cleanly, dev server returns 200, CSS bundle contains the project's dark hex values, all components render.
- **Spec 02 — Editor chrome components + route** (commit pending)
  - `components/editor/editor-navbar.tsx` — top navbar (`h-14`, `bg-base`, bottom border), three-section flex layout, sidebar toggle using `PanelLeftOpenIcon` / `PanelLeftCloseIcon` (lucide v1.x `Icon` suffix). Client component, props `{ isOpen, onToggle }`.
  - `components/editor/project-sidebar.tsx` — floating left sidebar (`fixed left-0 top-14 bottom-0 z-40`, `w-72`, `bg-base/95 backdrop-blur-md`, `border-r border-surface-border`). Slides in via `translate-x` transform + `transition-transform duration-200`. Header with `Projects` title + close button. Body uses shadcn `Tabs` (`My Projects` / `Shared`) with empty-state placeholders. Footer is a full-width `New Project` `Button` with `PlusIcon`. Client component, props `{ isOpen, onClose }`.
  - `components/editor/dialog.tsx` — `EditorDialog` namespace that re-exports the shadcn `Dialog` primitive with project modal styling applied to `Content` (`rounded-3xl`, `bg-base`, `text-copy-primary`). Preserves the shadcn animation classes and `showCloseButton` behavior. `Footer` mirrors the shadcn footer with `rounded-b-3xl` and the project border token. Future dialogs import from `@/components/editor/dialog`.
  - `components/editor/index.ts` — barrel re-exporting `EditorNavbar`, `ProjectSidebar`, and the `EditorDialog` namespace.
  - `app/editor/page.tsx` — client-component route. Owns `useState<boolean>` (default `true`) for sidebar open/close, renders `<EditorNavbar />` + floating `<ProjectSidebar />` + a centered `<main>` canvas placeholder (`text-copy-muted` "Canvas" label). Wrapped by the root `app/layout.tsx`; no `app/editor/layout.tsx` for now.
  - No new dependencies, no `globals.css` changes, no modifications to `components/ui/*`.
  - Verified: `bunx tsc --noEmit` exits 0, `bun run lint` (oxlint) exits 0, `bunx oxfmt --check components/editor/ app/editor/` clean. Smoke-tested: dev server boots, `GET /editor` returns 200 (~20KB, ~175ms warm), rendered HTML contains `Projects`, `My Projects`, `Shared`, `New Project`, and `Canvas` markers; root `/` still 200, no regression.
- **Spec 03 — Clerk authentication** (commit pending)
  - `@clerk/ui@1.31.0` installed as a runtime dep (the theme package; `@clerk/nextjs@^7.8.3` was already on the working tree).
  - `proxy.ts` at project root — Next 16's renamed Middleware. `clerkMiddleware` from `@clerk/nextjs/server` with a public-route matcher for `/sign-in(.*)` and `/sign-up(.*)`; every other route calls `auth.protect()`. The public-route patterns mirror the `NEXT_PUBLIC_CLERK_SIGN_IN_URL` / `NEXT_PUBLIC_CLERK_SIGN_UP_URL` env vars so a single source of truth drives both. Matcher excludes `_next/static`, `_next/image`, `favicon.ico`, and image extensions; runs on `(api|trpc)(.*)`.
  - `app/sign-in/[[...sign-in]]/page.tsx`, `app/sign-up/[[...sign-up]]/page.tsx` — server components. Catch-all routing so Clerk's multi-step subroutes (`/sign-in/factor-one`, `/sign-in/verify-email-address`, etc.) work. Each page renders `<AuthShell><SignIn|SignUp appearance={authAppearance} routing="path" ... /></AuthShell>`.
  - `components/auth/auth-shell.tsx` + `index.ts` — `"use client"` presentational two-panel shell: left aside (logo + tagline + 3-item feature list, `hidden` below `lg`) + right main (centered `max-w-sm` form slot). No business logic. No modifications to `components/ui/*`.
  - `lib/auth-appearance.ts` — single source of truth for Clerk appearance. Uses the `shadcn` theme from `@clerk/ui/themes` (per Clerk skill mandate for shadcn projects) and a `variables` map that aliases every Clerk CSS variable to the project's existing `var(--...)` tokens (`colorPrimary` → `var(--accent-primary)`, etc.). No hardcoded hex anywhere in auth files. The matching `@import "@clerk/ui/themes/shadcn.css"` line in `app/globals.css` (alongside the existing Tailwind imports) is what actually emits the shadcn-token-mapped classes into the bundle — without it, Clerk's profile / "Manage account" pages fall back to light defaults.
  - `app/layout.tsx` — now wraps children in `<ClerkProvider>`; `RootLayout` is `async` (ClerkProvider returns `Promise<JSX.Element>` in v7). Metadata updated to `Ghost AI / Collaborative AI design canvas`.
  - `app/page.tsx` — server component reading `auth()` from `@clerk/nextjs/server`; redirects `/editor` when signed in, `/sign-in` when signed out. Acts as a safety net for client-side navigations (the proxy is the primary gate).
  - `components/editor/editor-navbar.tsx` — right flex section now renders `<UserButton appearance={authAppearance} />` (Clerk's default user menu/profile flows kept intact; only the post-logout destination is configured globally via `NEXT_PUBLIC_CLERK_SIGN_OUT_FALLBACK_REDIRECT_URL` if/when set).
  - `.env.local` — added `NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in` and `NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up`. (Publishable and secret keys were already in place.)
  - No changes to `globals.css` or any `components/ui/*` file.
  - Verified: `bun run typecheck` (tsc --noEmit) exits 0, `bun run lint` exits 0, `bun run build` produces 5 routes + `Proxy (Middleware)` in the build output and exits 0. `bun run fmt:check` is clean on all new files; the 22 pre-existing drift files in `.claude/`, `.agents/`, and the spec markdown are unchanged. Dev server smoke test: `/sign-in` returns 200 with the two-panel layout (renders "Ghost AI", "Think out loud, ship out loud.", the 3-item feature list, and the Clerk form mount point); `/editor` returns 307 to `/sign-in?redirect_url=...` (proxy working); `/` is dev-mode-rewritten by Clerk to its dev-browser handshake page (expected behavior — production redirects to `/sign-in`).
- **Spec 05 — Prisma data models, client singleton, first migration** (commit pending)
  - `prisma/models/project.prisma` (new) — `ProjectStatus` enum (`DRAFT`, `ARCHIVED`), `Project` model (`id` `cuid()`, `ownerId String`, `name`, `description String?`, `status ProjectStatus @default(DRAFT)`, `canvasJsonPath String?`, `createdAt`/`updatedAt`, `@@index([ownerId])`, `@@index([createdAt])`) and `ProjectCollaborator` model (`id`, `projectId`, `email`, `createdAt`, `@relation(... onDelete: Cascade)`, `@@unique([projectId, email])`, `@@index([email])`, `@@index([projectId, createdAt])`). No `User` model — `ownerId` is a Clerk user id string, not an FK.
  - `prisma/schema.prisma` (modified) — slimmed to just the `generator` (provider `prisma-client`, output `../app/generated/prisma`) and `datasource db { provider = "postgresql" }` blocks. Multi-file schema: models live in `prisma/models/`, declarations stay in the root.
  - `lib/prisma.ts` (new) — cached Prisma client singleton. Branches on `DATABASE_URL` prefix: `prisma+postgres://` → `new PrismaClient({ accelerateUrl }).$extends(withAccelerate())` (local `prisma dev` and remote Prisma Postgres); everything else → `new PrismaPg({ connectionString })` + `new PrismaClient({ adapter })`. Throws on missing `DATABASE_URL`. Dev hot-reload cache typed via `interface GlobalForPrisma` (no `as unknown as` cast). Imports from `../app/generated/prisma/client` (the `prisma-client` generator's server entrypoint).
  - `@prisma/extension-accelerate@^3.0.1` added to `dependencies` (the package's version track is independent of the Prisma 7 line; the spec was written assuming `^7.8.0` but the package's current stable is `3.0.1`).
  - First migration applied: `prisma/migrations/20260902122814_init_project_models/migration.sql` — `CREATE TYPE "ProjectStatus"`, two `CREATE TABLE` statements (with `UNIQUE` and `ON DELETE CASCADE` constraints), all four `@@index` declarations, and the cascade FK to `Project.id`. Client generated at `app/generated/prisma/` (`client.ts`, `browser.ts`, `enums.ts`, `models.ts`, `models/Project.ts`, `models/ProjectCollaborator.ts`, `internal/*`) — already covered by the existing `/app/generated/prisma` gitignore entry.
  - `prisma dev` is now running as a background server (registered name `default`, main port 51213, direct db 51214). All future migration commands will work without re-starting it.
  - No changes to `lib/projects.ts` or `hooks/use-project-dialogs.ts` — the dialog hook is the seam for the future swap, left untouched per spec scope ("no API calls or persistence" stance from spec 04 + the spec-05 scope discipline). No `app/api/` routes created (out of scope). No seed data (out of scope).
  - Verified: `bun run typecheck` (tsc --noEmit) exits 0, `bun run lint` (oxlint) exits 0, `bun run fmt:check` clean on all 316 files, `bun run build` produces 5 routes + `Proxy (Middleware)` and exits 0. Initial typecheck failed on `lib/prisma.ts` because the Accelerate branch returns a wider `PrismaClientExtends<...>` type than the plain `PrismaClient` annotation allowed (`$on` is missing on the extended type); fix was to drop the explicit return-type annotation and let TypeScript infer the union of both branches.
- **Spec 04 — Project dialogs & sidebar actions** (commit pending)
  - `bunx shadcn@latest add dropdown-menu` — one new shadcn primitive added to `components/ui/dropdown-menu.tsx` (unmodified per the protected-foundation rule). The other shadcn primitives needed (`Dialog`, `Input`, `Button`, `Tabs`, `ScrollArea`) were already installed.
  - `lib/projects.ts` — `Project` interface (`id`, `name`, `slug`, `ownerId`, `isOwner`, `collaborators?`), pure `slugify(name)` helper, and a `mockProjects` array (2 owned: "Auth Service Redesign", "Event Pipeline"; 1 shared: "Platform Roadmap"). No persistence.
  - `hooks/use-project-dialogs.ts` — new `hooks/` directory (matches the `components.json` alias). Client hook owning the project list (`useState<Project[]>` initialized from `mockProjects`), dialog state as a discriminated union (`{ type: "create" } | { type: "rename"; project } | { type: "delete"; project } | { type: null }`), shared `formName` and `isSubmitting`. Public API: `openCreate` / `openRename` / `openDelete` / `closeDialog`, `setFormName`, `submitCreate` / `submitRename` / `submitConfirmDelete` (each closes the dialog and resets state on success). ID generation uses `crypto.randomUUID()` with a fallback for SSR safety. Hook colocates all dialog state so the page-level editor route mounts all three dialogs once and the home "New Project" CTA + sidebar "New Project" button share the same open handler.
  - `components/editor/project-item.tsx` — sidebar row. Name on the left; owned items get a `MoreHorizontalIcon` ghost button (opacity 60 → 100 on row hover) opening a shadcn `DropdownMenu` with `PencilIcon` Rename + `TrashIcon` Delete (the latter uses the new `variant="destructive"` on `DropdownMenuItem` — picks up `text-destructive` and destructive focus bg). Shared/collaborator items render the name only — no menu — per the spec's "hide actions for shared projects" rule.
  - `components/editor/create-project-dialog.tsx` — controlled dialog using `EditorDialog`. Name `Input` with `autoFocus` + a live slug preview below it (`Slug: <span className="font-mono">{slug}</span>`, recomputed each render from `formName` — no debounce). Footer Cancel + Create (`type="submit"`, `disabled={!formName.trim() || isSubmitting}`). Submit on `<form onSubmit>` so Enter creates.
  - `components/editor/rename-project-dialog.tsx` — controlled dialog. Description shows the current project name for context. Input `autoFocus` + `onFocus={(e) => e.currentTarget.select()}` so a single keystroke replaces the existing name. Save is `type="submit"` (Enter submits). Submit is disabled when the trimmed name is empty, unchanged, or submitting.
  - `components/editor/delete-project-dialog.tsx` — destructive confirmation. `EditorDialog.Content showCloseButton={false}` (destructive flows shouldn't be dismissed casually). Description names the project and includes "This action cannot be undone." Footer Cancel + Delete (`variant="destructive"`, no form, direct `onClick`).
  - `components/editor/project-sidebar.tsx` — modified to accept `ownedProjects`, `sharedProjects`, `onCreate`, `onRename`, `onDelete` props. TabsContent bodies now render a `ScrollArea` of `ProjectItem`s or the existing "No projects yet." / "No shared projects yet." placeholders. Footer `New Project` button wired to `onCreate`. **Mobile backdrop**: a sibling `<button>` (not `<div>`, for keyboard a11y) at `z-30` (sidebar is `z-40`) with `md:hidden` — clicking it closes the sidebar. The X close and the navbar toggle continue to work at all breakpoints; the backdrop is just the additional mobile scrim + click-outside affordance.
  - `app/editor/page.tsx` — `Canvas` placeholder replaced with the home content: heading (`Create a project or open an existing one`, `text-xl font-medium text-copy-primary`), description (`text-sm text-copy-muted`), and a primary `Button` with `PlusIcon` opening the Create dialog. No card wrapper per spec. The page instantiates `useProjectDialogs()`, mounts the three dialogs alongside the sidebar, and passes `openCreate` to both the home CTA and the sidebar CTA.
  - `components/editor/index.ts` — barrel now re-exports `CreateProjectDialog`, `RenameProjectDialog`, `DeleteProjectDialog`, `ProjectItem` plus their prop types, alongside the existing `EditorNavbar` / `ProjectSidebar` / `EditorDialog`.
  - No `globals.css` changes, no new third-party runtime dependencies (only the shadcn CLI generated the new `dropdown-menu.tsx`).
  - Post-implementation fix: the `Input` in `create-project-dialog.tsx` and `rename-project-dialog.tsx` got `className="bg-surface text-copy-primary placeholder:text-copy-muted"`. The shadcn `Input` primitive uses `bg-transparent` and inherits color from the ancestor; native `<input>` color inheritance is unreliable, so the entered text + placeholder were nearly invisible against the `bg-base` modal. Adding `bg-surface` gives a clear visual boundary, and the explicit `text-copy-primary` + `placeholder:text-copy-muted` ensure high contrast in both states. The primitive itself was untouched (per the protected-foundation rule) — the override is scoped to the two dialog consumers that need it.
  - Verified: `bun run typecheck` (tsc --noEmit) exits 0, `bun run lint` (oxlint) exits 0, `bun run fmt:check` clean on all 9 new/modified app files. The two pre-existing files flagged by the formatter (`.claude/context/specs/04-project-dialogs.md` — the spec input — and the shadcn-generated `components/ui/dropdown-menu.tsx`) are intentionally left untouched per the surgical-changes and protected-foundation rules.

## In Progress

- None.

## Next Up

- **Real-time canvas surface** (spec 05): Liveblocks room + React Flow integration with the eight node color pairs and six shapes from `ui-context.md`, plus the right slide-over AI sidebar shell.

## Open Questions

- Pre-existing CRLF line endings in `.oxlintrc.json`, `CLAUDE.md`, and `commitlint.config.js` made `bun run fmt:check` fail. Worked around by adding them to `.oxfmtrc.json` `ignorePatterns` rather than reformatting them (surgical-changes rule). Worth a follow-up to either normalize the files to LF or document the CRLF convention in the project setup.
- `oxfmt --check` now also reports formatting drift in `.claude/commands/ship-feature.md` and `.claude/context/specs/02-editor.md` (pre-existing, not introduced by spec 02). Either re-run the formatter on these files in a follow-up or extend `ignorePatterns` if the drift is intentional.
- Clerk deprecation warning at dev-server boot: `createRouteMatcher` is deprecated and will be removed in the next major release. The spec 03 implementation uses it per the spec's "default-deny everything except public routes" pattern. When Clerk v8 lands, the migration target is resource-based auth checks (`auth().protect()` in each page/layout/server function that accesses protected data) — which would also let us drop the `proxy.ts` file entirely. Worth tracking but no action required now.
- Spec numbering mismatch: this tracker's "Next Up" entry currently describes the real-time canvas surface (Liveblocks + React Flow) and labels it as spec 05, but the actual `05-prisma.md` file is the Prisma data-model spec (now complete). The real-time canvas is presumably a later spec number that hasn't been written yet. Reconcile in a follow-up by either renumbering the spec files or rewriting the tracker's "Next Up" to point at the real `05-prisma.md`.

## Architecture Decisions

- shadcn is installed with the new `radix-nova` preset and the consolidated `radix-ui` package (no individual `@radix-ui/react-*` packages). Components are generated into `components/ui/` at the project root (no `src/`).
- Dark is the only mode. `globals.css` sets the same dark palette on both `:root` and `.dark` so shadcn's `dark:` variants and the default-light classes resolve to the same dark values. `<html>` carries the `dark` class permanently.
- The shadcn variable names (`--background`, `--primary`, etc.) and the project's variable names (`--bg-base`, `--text-primary`, `--accent-primary`, etc.) coexist in `:root` so generated components and app-level code can each use their preferred naming without conversion.
- `components/ui/*` are treated as protected third-party foundation components. Project-specific styling lives in `app/`, `components/` (composables), or future feature-level wrappers — never inside the generated shadcn files.
- The shadcn `DialogContent` hardcodes `rounded-xl` and `bg-popover` in its className argument. The editor's `EditorDialog.Content` re-implements the same surface with project defaults (`rounded-3xl`, `bg-base`) applied — preserving animations and `showCloseButton` behavior. This is the pattern for any future shadcn primitive that needs project-style overrides without modifying the protected primitive.
- Clerk uses the `shadcn` theme from `@clerk/ui/themes` (not the `dark` theme), because the project has shadcn installed. The `shadcn` theme is a prebuilt appearance that reads the standard shadcn CSS variable namespace (`--card`, `--card-foreground`, `--primary`, `--foreground`, `--muted`, `--input`, `--border`, `--ring`, `--destructive`, etc.) — these are already declared in `app/globals.css` `:root` and `.dark` with the project's dark hex values, so the theme inherits the dark palette automatically. The matching `@import "@clerk/ui/themes/shadcn.css"` line in `app/globals.css` (alongside the existing Tailwind imports) is what actually emits the shadcn-token-mapped classes into the bundle. Without that import, Clerk's "Manage account" / profile pages fall back to light defaults. **Do NOT override the `variables` block in the Clerk appearance with project-specific token names** (e.g. `--bg-base`, `--accent-primary`) — the per-component `appearance.variables` block **replaces** the theme's defaults rather than merging, so referencing non-shadcn variable names there causes UserProfile/AccountSettings to lose their shadcn-themed colors. Keep the `variables` block minimal (only `fontFamily` or similar non-conflicting keys) and put project-specific styling in the `elements` block instead, which is composable.
- The `<UserButton>`'s appearance only propagates to the **popover menu** by default — the "Manage account" modal it opens (rendered as a separate `<UserProfile>` component) needs the same appearance passed via the `userProfileProps={{ appearance: ... }}` prop. Without this, the popover renders dark (correct) but the user-profile modal inside it falls back to Clerk's default light theme. The fix in `components/editor/editor-navbar.tsx` passes `authAppearance` to both the `appearance` prop and `userProfileProps.appearance` so the modal inherits the same theme.
- Next 16 renamed Middleware to Proxy — `proxy.ts` at the project root is the new convention. The function inside is still `clerkMiddleware` from `@clerk/nextjs/server`; only the file name and the documented "Proxy" terminology changed.
- The `ClerkProvider` in `@clerk/nextjs@7` is an async Server Component (`Promise<React.JSX.Element>`), so `RootLayout` is declared `async`. No client wrapper needed. Each Clerk surface (`<SignIn>`, `<SignUp>`, `<UserButton>`) receives the shared `authAppearance` per-instance, so the provider itself stays unopinionated and per-component `elements` overrides apply where needed.
- Spec 04 introduced the first `hooks/` directory at the project root, matching the `components.json` alias `@/hooks`. Feature-specific hooks (e.g. `useProjectDialogs`) live here; cross-cutting state primitives will land in `lib/` if they emerge. The hook colocates dialog state, form state, and the project list so the editor page is the single mount point for all three dialogs and the home CTA + sidebar CTA can share the same open handler.
- shadcn `Input` has no explicit text color and uses `bg-transparent`. Against a non-default background (e.g. inside `EditorDialog.Content` which is `bg-base`), the entered text + placeholder render nearly-invisible because native `<input>` color inheritance is unreliable. The established fix is to set `className="bg-surface text-copy-primary placeholder:text-copy-muted"` per consumer — gives a clear visual boundary, high-contrast text, and a de-emphasized but readable placeholder. The `Input` primitive itself is left untouched per the protected-foundation rule.

## Session Notes

- `shadcn@4.19.1` was the version installed; the spec asked for `^4.5.0` which is satisfied.
- `lucide-react@1.38.0` was installed (the `^1.11.0` caret resolved to the latest 1.x line). Some component examples in the wider ecosystem expect `lucide-react@0.x` API; the 1.x API is largely compatible but uses ESM-only distribution and renamed a few internal symbols.
- `tw-animate-css` is installed as a transitive dep of shadcn; `app/globals.css` imports it at the top alongside `tailwindcss` and `shadcn/tailwind.css`.
- The `radix-nova` preset uses `data-slot` attributes and `data-active` / `data-closed` state attributes (rather than `data-state`) for open/closed styling. Component usage documentation should reflect this.
- Spec 02 chose "components only, no route" so the chrome pieces can be reviewed and refined in isolation. The `useState`-based open/close wiring lands with the editor route in the next step — `EditorNavbar.onToggle` and `ProjectSidebar.onClose` are the two callbacks that will be connected.
- Updated mid-spec: the user asked for the editor route to land in the same spec, so `app/editor/page.tsx` was added in this iteration rather than deferred. The page is a single client component (`h-dvh flex flex-col` shell) with no `app/editor/layout.tsx` — if a future editor-scoped provider stack accumulates, that's the natural extraction point.
- `oxfmt --check` currently reports formatting issues in `.claude/commands/ship-feature.md` and `.claude/context/specs/02-editor.md` (pre-existing). Left untouched per the surgical-changes rule. Should be normalized in a follow-up.
- The Clerk v7 `Variables` type uses different keys than the v6 / older docs (e.g. `colorInput` not `colorInputBackground`, `colorInputForeground` not `colorInputText`, `colorForeground` not `colorText`, `colorMutedForeground` not `colorTextSecondary`, `colorMuted` not `colorMutedBackground`). The `authAppearance` config maps the project tokens to the v7 names. `UserButton` no longer accepts `afterSignOutUrl` as a prop — post-logout destination is configured via `NEXT_PUBLIC_CLERK_SIGN_OUT_FALLBACK_REDIRECT_URL` env var or the `ClerkProvider` `signInFallbackRedirectUrl` prop. Left for a follow-up if a non-default post-logout page is needed.
- The Clerk v7 type `ClerkAppearanceTheme` lives in `@clerk/shared/types` (re-exported via `@clerk/shared`); `@clerk/types` is a different older package that is not a direct dep here. The `authAppearance` is typed against `@clerk/shared/types` for that reason.
- Dev-server smoke test on `/` shows Clerk's dev-browser handshake page (404 with `x-clerk-auth-reason: protect-rewrite, dev-browser-missing`) — this is the expected dev-mode behavior because no Clerk dev browser cookie is set. In production with a real auth session, the proxy redirects unauthenticated users to `/sign-in`; the page-level `auth()` redirect in `app/page.tsx` is the safety net for client-side navigations.
- Spec 04 introduced the first project hook at `hooks/use-project-dialogs.ts` and a new `hooks/` directory at the project root (matching the `@/hooks` alias in `components.json`). The hook colocates dialog state, form state, and the project list so the page is the single mount point for all three dialogs. When the project list graduates to a real store (Prisma + Clerk-derived `ownerId`), the hook is the seam — the consumers don't change.
- The shadcn `Input` primitive's default `bg-transparent` + no explicit text color looked correct on paper (inherited `text-foreground` against `bg-base`) but rendered nearly-invisible text inside the modals because native `<input>` color inheritance is unreliable. Fix was per-consumer `className="bg-surface text-copy-primary placeholder:text-copy-muted"` in the two dialogs that use `Input` — the primitive itself is untouched per the protected-foundation rule. Any future `Input` consumer that needs the same treatment should use this pattern.
- Spec 04 used Tailwind-only mobile detection (`md:hidden` on the backdrop) rather than a `useMediaQuery` hook. The backdrop is a `<button>` (not `<div>`) so keyboard users get the close action for free; clicking anywhere outside the sidebar on mobile closes it. On `md`+ viewports the backdrop is hidden and the existing X close / navbar toggle keep working.
- Spec 05: `@prisma/extension-accelerate` is on its own version track — the latest stable is `3.0.1` and `^7.8.0` does not resolve. The package is API-compatible with Prisma 7 (the `withAccelerate()` extension + `accelerateUrl` constructor option work the same). Worth keeping in mind if any future spec upgrades Prisma further.
- Spec 05: `bunx prisma migrate dev --name <name>` did NOT auto-run `prisma generate` in this environment. The migration applied, the SQL file landed, but the generated client tree at `app/generated/prisma/` was not written until `bunx prisma generate` was run explicitly. Future specs that depend on freshly added models should run both commands, or chain them (`bunx prisma migrate dev && bunx prisma generate`).
- Spec 05: the `as unknown as` cast that the v7 driver-adapter skill suggests for the `globalThis` hot-reload cache does not type-check under the strict TS config here (the `PrismaClientExtends<...>` returned by `withAccelerate()` is wider than the base `PrismaClient`, and the cast `as unknown as { prisma?: PrismaClient }` makes the cache narrower than the actual value). The working pattern is to drop the explicit return-type annotation, let TypeScript infer the union of both branches, and type the `globalThis` slot as `ReturnType<typeof createPrismaClient>`. Avoids the `any` / `unknown` cast entirely.
